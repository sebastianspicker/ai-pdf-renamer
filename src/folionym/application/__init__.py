"""Application orchestration for Folionym's local rename workflows.

The package owns discovery, proposal production, scheduling, side effects,
and conventional batch operations. Filesystem mutation remains in
``folionym.rename_ops``.
"""
